<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.kyj.cooltiger.cooltigerproduct.mapper.ProductServiceMapper">

    <!-- 添加商品服务 -->
    <insert id="addProductService" parameterType="ProductService">
        INSERT INTO ky_product_service
            (name, details, service_type, product_id)
        VALUES
        (#{productService.name}, #{productService.details},
         #{productService.serviceType}, #{productService.productId})
    </insert>

</mapper>