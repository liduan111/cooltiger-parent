<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.kyj.cooltiger.product.mapper.ProductFreightMapper">

    <!-- 添加商品运费 -->
    <insert id="addProductFreight" parameterType="ProductFreight">
        INSERT INTO ky_product_freight
        (product_id, product_freight_type, condition, preight_price, first_weight,
         continue_weight, whether_kg)
        VALUES
        (#{productFreight.productId}, #{productFreight.productFreightType}, #{productFreight.condition},
         #{productFreight.preightPrice}, #{productFreight.firstWeight}, #{productFreight.continueWeight},
         #{productFreight.whetherKg})
    </insert>

    <!-- 删除商品运费 -->
    <delete id="deleteProductFreightByProductId">
        DELETE FROM ky_product_freight WHERE product_id = #{productId}
    </delete>
</mapper>