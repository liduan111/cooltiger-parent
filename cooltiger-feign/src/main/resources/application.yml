#服务端口
server:
  port: 8889

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8888/eureka/eureka/

spring:
  application:
    name: Feign-Consumer
  main:
    allow-bean-definition-overriding: true

ribbon:
  eureka:
    enabled: true